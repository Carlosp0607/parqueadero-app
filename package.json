{
  "name": "parqueadero",
  "version": "1.0.0",
  "description": "Sistema de gestión de parqueadero",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build:clean": "rimraf dist && mkdirp dist",
    "build:exe": "pkg src/server.js --targets node18-win-x64 --output dist/parqueadero.exe",
    "build:copy": "cpy \"public/**\" dist/public --cwd=. --parents && cpy schema.sql .env* dist/ --cwd=.",
    "build": "npm run build:clean && npm run build:exe && npm run build:copy",
    "test": "node --test tests/*.test.js"
  },
  "keywords": [],
  "author": "Cristian Cano",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.9.1"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "mkdirp": "^3.0.1",
    "nodemon": "^3.1.10",
    "pkg": "^5.8.1",
    "rimraf": "^5.0.7"
  },
  "engines": {
    "node": ">=18"
  }
}