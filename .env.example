# Copie este archivo como .env y complete los valores reales.
# NUNCA suba el archivo .env al repositorio.

PORT=3000

# Base de datos
DB_HOST=localhost
DB_USER=parqueadero_app
DB_PASSWORD=CAMBIAR_POR_CLAVE_FUERTE
DB_NAME=parqueadero

# Secreto JWT. Genérelo con:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=CAMBIAR_POR_SECRETO_ALEATORIO_DE_96_CARACTERES
